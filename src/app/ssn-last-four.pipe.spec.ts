import { SsnLastFourPipe } from './ssn-last-four.pipe';

describe('SsnLastFourPipe', () => {
  it('create an instance', () => {
    const pipe = new SsnLastFourPipe();
    expect(pipe).toBeTruthy();
  });
});
